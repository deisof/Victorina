"I love " + "python"\\"I love python"\\оператор + в языке програмирования python в работе со строками склеивает их, поэтому правильный ответ "I love python"
6 == 12\\False\\ оператор двойного равенства сравнивет значения, и в результате мы получаем True или False, 6 не равно 12,следовательно, выражение 6 == 12 неверно, False
4.5 // 2\\2\\оператор // в python целочисленное деление, поэтому в ответе у получается 2
type(1 / 2)\\class 'float'\\функция type() в python возращает тип переменной, в данном случае после нецелочисленного деления 1 на 2 получаем 0.5, у значения этого выражения тип float 
"snow storm"[6:8]\\to\\в данном примере есть строка "snow storm", у которой мы берём срез [6:8] с 7 по 8 включительно, в итоге, мы получаем строку "to"
len([1, 2, 3, None, (), [], ])\\6\\функция len() в python возращает размер аргумента,в данном случае в списке 6 элементов, поэтому ответ 6
0 if 23 > 10 else 11\\0\\здесь условие if(если) 23 >(больше) 10 и else(иначе) 11, else срабатывает, если условие if неверно(False), здесь условие верно, поэтому 0
"Hi" * 4\\HiHiHiHi\\происходит дизъюнкиця, оператор * дублирует строку определённое количество раз, поэтому ответ HiHiHiHi
eval('300 / 10 * 10')\\300.0\\оператор / это оператор вещественного деление, который возвращает объект типа float, а функция eval() вычисляет выражение переданное, как аргумент, поэтому ответ 300.0
"Alice says I python love"[20:-1] + "I love"[-1] * eval('50 //10')\\loveeeee\\здесь берётся срез строки с 20 по последний элемент(-1, не включительно), затем соединяется с последним элементом строки [-1](последний элемент), и дублируется этот элемент на столько раз, сколько получится после целочисленного деления 50 на 10, и в итоге мы получаем строку loveeeee
